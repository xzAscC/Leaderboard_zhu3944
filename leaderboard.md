# Leaderboard — Speech Emotion Recognition Challenge

> Last updated: **—**
> Baseline weighted F1: **TBA**
> Metric: weighted F1-score on the public test set

---

No submissions yet. Submit your results by running:

```bash
python test.py \
    --test_dir    dataset/test \
    --results_dir results \
    --team_name   "Your Team Name"
```

Then commit and push `Your_Team_Name.json` from your `results/Your_Team_Name/` folder to the `submissions/` folder of this repository.

---

*This leaderboard updates automatically when teams push a new JSON file to `submissions/`.*
